package net.techgy.cmty.ui.remoteobject;

public class MapOperatorHandler { /*extends AbstractOperationHandler{

	private VoyagerMap map = null;
	private  static final String CLIENT_CALL = "clientCall";
	public MapOperatorHandler(VoyagerMap map) {
		this.map = map;
	}
	
	@Override
	public void handleSet(JsonObject properties) {
		super.handleSet(properties);
	}

	@Override
	public void handleCall(String method, JsonObject parameters) {
		if(CLIENT_CALL.equals(method)) {
			String msg = parameters.get("msg").toString();
			map.clientCall(msg);
		}
	}

	@Override
	public void handleNotify(String event, JsonObject properties) {
		super.handleNotify(event, properties);
	}*/
}
